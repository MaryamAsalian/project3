import React from 'react';
import Loadin from '../gif/loading.gif'

const Loader = () => {
    return (
        <div>
            <img src ={Loadin} alt = "ssss" />
            <h1>loading...</h1>
        </div>
    );
};

export default Loader;